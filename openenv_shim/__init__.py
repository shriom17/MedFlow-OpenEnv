"""Compatibility shim for OpenEnv minimal interfaces.

This package provides tiny definitions so the project can run without the
full `openenv` dependency (useful for Colab demos or quick local runs).
"""

__all__ = ["core"]
